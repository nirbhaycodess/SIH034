# ── Core
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic[email]==2.10.5
pydantic-settings==2.7.1

# ── Database
pymongo[srv]==4.10.1

# ── Auth
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# ── File uploads
python-multipart==0.0.20
Pillow==11.1.0
aiofiles==24.1.0

# ── Image / OCR (optional at runtime; guarded with try/except)
opencv-python-headless==4.10.0.84
numpy==1.26.4
# paddleocr==2.9.1        # uncomment to enable PaddleOCR
# paddlepaddle==3.2.2     # uncomment to enable PaddleOCR

# ── AI
google-generativeai==0.8.4

# ── PDF
reportlab==4.2.5

# ── Dev / Quality
python-dotenv==1.0.1
ruff==0.9.3
black==24.12.0

# ── Testing
pytest==8.3.4
pytest-asyncio==0.25.2
httpx==0.28.1
